[build]
builder = "nixpacks"
buildCommand = "./gradlew buildFatJar"

[deploy]
startCommand = "java -jar build/libs/todapp-0.0.1-all.jar"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
